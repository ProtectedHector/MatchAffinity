{% extends 'common/base.html.twig' %}
{% import "macros/forms.html.twig" as forms %}

{% block body %}
    Available teams:
    {% for team in teams %}
        {{ team.name }},
    {% endfor %}
    <hr>
    {{ form_start(form) }}

    {{ forms.form_field(form.name) }}
    {{ forms.form_field(form.competition) }}
    {{ forms.form_field(form.flagPath) }}
    {{ forms.form_field(form.submit) }}

    {{ form_end(form) }}
{% endblock %}

