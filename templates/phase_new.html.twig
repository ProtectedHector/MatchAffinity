{% extends 'base.html.twig' %}

{% block body %}
    Available phases:
    {% for phase in phases %}
        {{ phase.name }},
    {% endfor %}
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.submit) }}
    {{ form_end(form) }}
{% endblock %}

