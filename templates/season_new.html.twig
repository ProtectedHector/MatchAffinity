{% extends 'base.html.twig' %}

{% block body %}
    Available seasons:
    {% for season in seasons %}
        {{ season.name }},
    {% endfor %}
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.submit) }}
    {{ form_end(form) }}
{% endblock %}

